"""
This app previously contained the CustomUser model.

The custom user model has been moved to `bookshelf.models.CustomUser` and
`AUTH_USER_MODEL` is configured accordingly in `LibraryProject/settings.py`.

Keep this file present for app-specific models that may be added later.
"""
    